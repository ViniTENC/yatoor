# Constraints (SPEC intel)

No SPEC-type documents were found in this ingest batch. All 8 classified documents are type `DOC`.

No formal API contracts, schemas, NFRs, or protocols were classified as SPEC. However, `ESTADO_BD.md` (type DOC) contains a de-facto schema snapshot; see `context.md` topic "Supabase — estado de la base" for the current table/function inventory. This is descriptive status, not a locked contract, and should not be treated as a SPEC-precedence source.
