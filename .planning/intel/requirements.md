# Requirements (PRD intel)

No PRD-type documents were found in this ingest batch. All 8 classified documents are type `DOC`.

No formal requirements/acceptance-criteria to record from this batch. See `context.md` for the operational runbook (`nueva-ciudad.md`) which describes the de-facto process for adding city data — this could be promoted into formal requirements by the roadmapper if desired, but is recorded here as context, not as a PRD-derived requirement.
